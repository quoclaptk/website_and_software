<span class="btn btn-success fileinput-button pull-left">
    <i class="glyphicon glyphicon-plus"></i>
    <span>{{ word._('_chon_hinh') }}...</span>
    <input class="fileupload" type="file" name="files" data-id="{{ id }}" data-type="{{ type }}" data-folder="{{ folder }}">
</span>
<div id="files_{{ type }}_{{ id }}" class="files ajaxfileuploadimg clearfix pull-left"></div>